import "package:flutter/material.dart";

class validation extends StatelessWidget {
  final VoidCallback selectHandler;

  // Firebase function should return valid or not 
  
  Answer(this.selectHandler)
